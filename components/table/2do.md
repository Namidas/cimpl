- need to think if I want transform do utils/defaults into some sort of reactive on itself (it's a plain object with getter/setter functions right now), but most likely add a method getReactive that automatically wraps the getter on a computed or something of the sorts (seems to make more sense, would also make it retrocompatible as-is) [but this is actually not tables related]
or maybe do it exactly the other way around and make it reactive by default and have getValue (that returns a toValue()) ?? in a ES6 context it makes more sense to have it non-reactive by default, but in a vue3 context it makes more sense the other way around...

- I need a "free caches" sort of thing on unMount, specially for defaults (which are external to the compo, so they won't get garbage collected), but I should run some tests and see in exactly which ways everything breaks down so I can define the propper way of handling each edge-case
actually the defaults.getWrapper param "cache" defaults to false, so every parent compo gets it's own instance and that gets GC-ed...so maybe we should leave that "cache" to default as false and get slighlty more overhead but encapsulated on stuff that gets dispossed of automatically, specially for behaviors in general (for instance, tables, how many tables are you going to show at once on screen so that it makes sense to cache defaults?), "theme" related...maybe set the cache manually to true, since that tends to be use everywhere around

- there's also another problem with caches of defaults, haven't tested it but no need to, those caches are generated "globally" but based on a specific table, so the "second" table (at whichever point) will have wrong data...somehow related, something I've been thinking for quite some time now, I've included a lot of configuration options on the defaults that there's no other way to change (essentially and for instance, no prop to change sorting icons), so to keep things "elegant" I'll need to "downgrade" those to individual props and internally build the final props, I could also have a single prop "defaults" or "config" to pass down an entire object...and if we put these 2 points together the first thing is to differentiate "defaults" from "config" actually, right now the first one is being used as the second one....what I think I should do, use an internal tableConfig (instead of table defaults) that's essentially a _.merge(defaults,props.config,{props specifics for each possible config}) and have an utils of some sort that creates what essentially defaults is (with getters, setters, but most importantly caches) but on a separate object, so that caches don't conflict between tables, and also solves the problem of getting all those caches garbage collected when the table gets unmounted